using System.Diagnostics;

public enum Status
{
    // These are the statuses in the game. Each one will trigger different parts of the battle class when applied to the character's status.
    FINE, WEAK, PARALYZED, BURNED
}